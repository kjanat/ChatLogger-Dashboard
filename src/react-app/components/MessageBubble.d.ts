import { Message } from '../utils/api';
interface MessageBubbleProps {
    message: Message;
}
export default function MessageBubble({ message }: MessageBubbleProps): import("react/jsx-runtime").JSX.Element;
export {};
